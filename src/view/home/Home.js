import { loadAndRender } from '../../util/Render.js';

/**
 * Home.
 *  
 * @returns {undefined}
 */
export default function Home() {
    // Load and render the template
    loadAndRender('src/view/home/template.html', (html) => {        
    });
}