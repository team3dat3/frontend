import { loadAndRender } from '../../../util/Render.js';

/**
 * Contact.
 *  
 * @returns {undefined}
 */
export default function Contact() {
    // Load and render the template
    loadAndRender('src/view/home/contact/template.html', (html) => {        
    });
}