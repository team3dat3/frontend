import { loadAndRender } from '../../../util/Render.js';

/**
 * About.
 *  
 * @returns {undefined}
 */
export default function About() {
    // Load and render the template
    loadAndRender('src/view/home/about/template.html', (html) => {        
    });
}